---
project: <PROJECT_NAME>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
type: <bot | web | lib | script | other>
status: <active | paused | shipped | archived>
language: <python | typescript | go | ...>
mm_version: 0.1.0
---

# <PROJECT_NAME> — Passport

> Единый source-of-truth о проекте. Загружается в claude.ai → Project Knowledge.
> Читается всеми mm-* skills. Обновляется через `/mm-init-project` (режим update).

## 1. Идентификация

| Поле | Значение |
|---|---|
| Имя | <PROJECT_NAME> |
| Тип | <bot/web/lib/script> |
| Назначение | <одно предложение: что делает> |
| Стадия | <prototype/dev/prod/maintenance> |
| Путь к коду | `<абсолютный путь>` |
| Git remote | <url или `local-only`> |
| Ветка по умолчанию | `main` |
| Obsidian папка | `<абсолютный путь к Obsidian/Projects/<name>>` |

## 2. Стек

| Слой | Технология | Версия |
|---|---|---|
| Язык | | |
| Фреймворк | | |
| Менеджер пакетов | | |
| База данных / ORM | | |
| Тесты | | |
| Линтер / форматтер | | |
| Деплой | | |
| CI/CD | | |

**Ключевые зависимости (топ-5):**
- `<name>` — <зачем>

**Внешние сервисы:**
- <Telegram Bot API / OpenAI / Stripe / ...> — <как используется>

## 3. Архитектура

<2-4 предложения: какой паттерн, как разнесены слои, где главные абстракции.>

```
<дерево директорий до 2 уровней>
project/
├── src/
│   ├── ...
│   └── ...
├── tests/
└── ...
```

## 4. Точки входа

- `<file>` — <что запускает>
- `<file>` — <что запускает>

## 5. Команды

```bash
# Установка
<install command>

# Запуск (dev)
<run command>

# Тесты
<test command>

# Деплой
<deploy command>
```

## 6. Модели данных (если применимо)

| Сущность | Хранилище | Ключевые поля | Файл схемы |
|---|---|---|---|
| | | | |

## 7. Конвенции

- **Именование:** <snake_case для X, PascalCase для Y, ...>
- **Ошибки:** <как обрабатываются — exceptions, Result, и т.п.>
- **Состояние:** <stateless / FSM / context-managed>
- **Тесты:** <unit / integration / e2e — что обязательно>
- **Стиль UI** (для бота): <эмодзи-стиль из mm-config / другой>

## 8. Контекст для промптов ⚠️

> **Этот раздел — главный для Claude.** Перечисли 5-8 жёстких ограничений и неочевидных правил. Их сверяет каждый сгенерированный промпт.

- <Например: "Не менять структуру `core/router.py` — есть зависимости от внешнего API">
- <Например: "Все хендлеры — async, sync блокирует event loop">
- <Например: "База в SQLite через sqlmodel; не добавляй новые ORM">
- <Например: "Тексты UI — на русском, без англицизмов">
- <Например: "Логирование — через `loguru`, не `logging`">

## 9. Конфигурация и секреты

**ENV-переменные (только имена, без значений):**
- `<NAME>` — <что>

**Файлы конфига:**
- `<путь>` — <что хранит>

**Где лежат секреты:** `<.env / 1Password / ...>` — НЕ в репо.

## 10. Текущее состояние

- **В работе сейчас:** <одна строка>
- **Последняя сессия:** [[<obsidian_sessions>/<file>]]
- **Открытые вопросы:**
  - [ ] <q1>
  - [ ] <q2>
- **Известные баги:**
  - [ ] <bug1>

## 11. История версий паспорта

| Дата | Изменение |
|---|---|
| <YYYY-MM-DD> | Initial |

---

## ⚠️ Приватность — чек-лист перед загрузкой в claude.ai

Этот файл идёт в **Project Knowledge** на claude.ai (внешний сервис Anthropic).
Перед загрузкой убедись, что в нём НЕТ:

- [ ] API-ключей, токенов, паролей (Telegram, OpenAI, GitHub, etc.)
- [ ] URL'ов с inline-учётками (`https://user:pass@host`)
- [ ] Email/телефонов клиентов или коллег
- [ ] Внутренних URL'ов корп.инфраструктуры (если применимо)
- [ ] Имён сотрудников / партнёров / NDA-материалов

Имена ENV-переменных писать можно (`TELEGRAM_TOKEN`), значения — нельзя.
`/mm-init-project` автоматически прогоняет паспорт через secret-grep, но не верь скрипту на 100% — пробегись глазами.
